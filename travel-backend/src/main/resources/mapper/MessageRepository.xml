<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ai.travel.repository.MessageRepository">

    <select id="findByUserIdAndType" resultType="com.ai.travel.entity.Message">
        SELECT * FROM message
        WHERE user_id = #{userId}
        <if test="type != null and type != '' and type != 'all'">
            AND type = #{type}
        </if>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByUserIdAndType" resultType="java.lang.Long">
        SELECT COUNT(*) FROM message
        WHERE user_id = #{userId}
        <if test="type != null and type != '' and type != 'all'">
            AND type = #{type}
        </if>
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
    </select>

    <update id="markAsRead">
        UPDATE message SET is_read = 1, updated_at = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllAsRead">
        UPDATE message SET is_read = 1, updated_at = NOW()
        WHERE user_id = #{userId}
        <if test="type != null and type != '' and type != 'all'">
            AND type = #{type}
        </if>
        AND is_read = 0
    </update>

</mapper>
