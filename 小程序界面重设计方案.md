# 旅途派小程序界面重设计方案

## 目标与边界

保留现有信息架构、功能和主题色，不调整后端接口或业务流程。改版重点是用更强的层级、清晰的拇指区操作与路线化视觉语言，替代旧式的大面积圆角卡片、弱标题和分散入口。

## 设计令牌

| 令牌 | 浅色 | 深色 | 用途 |
| --- | --- | --- | --- |
| Brand | `#2EC4B6` | `#40D5C7` | 主操作、路线、选中态 |
| Brand deep | `#0F766E` | `#2EC4B6` | 顶部沉浸区、重点文字 |
| Accent | `#F97316` | `#F97316` | 预算、提醒、次要高亮 |
| Canvas | `#F7F5EF` | `#111318` | 页面底色 |
| Surface | `#FFFFFF` | `#1A1D26` | 内容面板 |
| Ink | `#1A1A2E` | `#E4E6EB` | 主文字 |

- 栅格：8 pt；页面边距 16 px；常用区块间距 24 px。
- 圆角：仅用于可点击控件与浮层（12/16/24 px），不再全屏使用大圆角白卡。
- 字体：`Noto Sans SC` 为中文正文，`DM Sans` 为数字、英文、日期和距离。
- 图标：延用现有线性 SVG；语义图标与文字同行，不用表情符号充当功能图标。
- 动效：页面进入 180ms 分层上移；按钮点击缩放至 0.98；底部抽屉 280ms 上滑。

## 全局组件体系

1. **路线标题栏**：左侧返回或城市，右侧行动图标；页面标题不再单独占用大块留白。
2. **探索搜索条**：搜索、地点和筛选合并为一条可展开的字段，筛选结果以数量反馈。
3. **路线摘要条**：日期、天数、预算、方式以紧凑信息胶囊呈现；用于规划、详情与地图。
4. **行程日时间线**：左侧连续路线线与时间，右侧为景点/住宿内容；日切换保持吸顶。
5. **底部行动栏**：每页只保留一个主行动；次要操作收进“更多”底部抽屉。
6. **内容状态组件**：加载、空、离线、权限、失败统一使用同一图形和行动文案。

## 逐页重设计

### 一级导航

| 页面 | 新布局与重点 |
| --- | --- |
| 发现 `index` | 顶部“当前城市 + 搜索”叠在低饱和地形纹理上；下方为可横滑的目的地、天数、主题筛选；瀑布流改为有节奏的双列行程编辑卡，封面占主视觉、作者和互动信息收敛到底部。右下固定“创建路线”。 |
| 规划 `planner` | 从长表单改为分段的“旅程构建器”：目的地与日期在首屏；偏好、交通、预算为可展开选择组；徒步模式作为独立路线分支，展示难度、海拔、体力和安全信息；主按钮固定为“让 AI 生成路线”。 |
| 行程 `my-trips` | 顶部显示下一段行程的倒计时/状态；“进行中、即将出发、已完成”分组并可切换；卡片以日期轨道和目的地为主体，常用操作放入滑动/更多菜单。 |
| 消息 `messages` | 以会话优先列表替代零散通知：置顶 AI 助手、行程提醒、互动消息三个分区；未读用品牌色细点，不再用大色块。 |
| 我的 `profile` | 信息头改为轻量档案区，头像与编辑入口并列；四项统计改为一条可点击数据轨；“下一次出发”置于首位，偏好和设置按主题分组，深色开关保留。 |

### 规划与行程链路

| 页面 | 新布局与重点 |
| --- | --- |
| AI 对话 `chat` | 首屏给出三条真实场景快捷问题和目的地检索；对话中的参数、生成进度、酒店和地图统一成“AI 建议块”，同一回合内连续排列；输入区固定，路线工作台可折叠为一条摘要。 |
| 行程详情 `trip-detail` | 封面高度缩短，标题、日期和作者叠在照片底部；收藏/评论/分享收为一行图标操作；路线、地图、景点、费用、评论五个 Tab 使用吸顶分段控件；路线内容用连续时间线，避免重复卡片。 |
| 当前行程 `current-trip` | 以“今天”为默认锚点：当前时间、下一站、到达提醒与一键导航置于首屏；日程按进行中/下一站/已完成区分；底部主操作是“开始导航”。 |
| 行程编辑 `publish-edit` | 使用分步编辑：基本资料、每日安排、住宿与发布设置；每一日为可拖拽的路线段，景点通过底部选择器添加；保存状态常驻标题栏。 |
| 地图 `map` | 地图全屏为底，顶部仅保留返回、交通模式和路径选择；底部半屏面板展示总距离、时间、海拔（徒步）与途经点；拖动面板在“摘要/站点/替代路线”间切换。 |
| 酒店列表 `hotel-list` | 搜索和日期改为吸顶紧凑栏；列表卡以价格、距离、位置、评分的扫描顺序组织；地图、电话、选择作为并排低干扰操作，已选择状态显著。 |
| 打包清单 `packing-list` | 进度环 + 出行日期置顶；物品按分类折叠，勾选使用细线与状态变色；新增入口固定在底部，支持根据天气/交通的 AI 建议标签。 |

### 社区与关系

| 页面 | 新布局与重点 |
| --- | --- |
| 发布 `publish-edit` / 我的发布 `my-publish` | 发布页突出封面、标题与可见范围；我的发布采用编辑型列表，展示浏览、点赞、评论和最后编辑时间，删除转入更多菜单。 |
| 用户主页 `userprofile` | 封面转为低高度氛围带，头像与名称部分错位；关注/私信固定在资料区；路线采用两列内容网格，信息控制在封面下两行。 |
| 关注 `following` | 用户条目改为头像、旅行偏好、最近路线和互相关系；取消关注保留二次确认，不做抢眼红色按钮。 |
| 足迹 `footprint` | 用中国/城市地图轮廓与时间轴表示去过的地点；城市卡包含次数、最近日期和收藏路线。 |
| 收藏 `favorites` | 顶部可切换“路线/景点/酒店”；空状态提供去发现页的明确入口；收藏信息保留来源作者与收藏时间。 |
| 私信 `imchat` | 消息气泡减弱，重点强化行程卡片、地点卡片和发送状态；输入栏支持快速发送当前路线。 |

### 账户与辅助页

| 页面 | 新布局与重点 |
| --- | --- |
| 登录 `login` | 品牌文字与短路线插画建立记忆点；手机号、验证码/密码改为单栏表单；游客模式降为辅助文字入口，协议确认与登录按钮的关系清楚。 |
| 编辑资料 `edit-profile` | 头像、昵称、所在地、简介按“个人识别—旅行标签—隐私”分组；底部固定保存按钮，并在输入变化时提示未保存。 |
| 积分 `points` | 首屏显示可用积分、成长路径与近期获取；任务与记录分 Tab，突出可执行任务而非单纯流水。 |
| 设置 `settings` | 分类为“账号、出行、通知、外观、关于”；开关行提供一句结果说明；危险操作独立置底。 |
| 帮助 `help` | 先展示高频问题和搜索，再按场景折叠；人工反馈入口固定在最后。 |
| 关于 `about` | 使用精简品牌档案、版本、隐私/协议和更新记录；去除信息卡重复边框。 |
| WebView `webview` | 顶部只保留原生返回、页面标题与外部链接标识；加载与网络失败采用统一状态。 |

## 页面状态与可访问性

- 所有主文字与背景至少达到 4.5:1 对比度；青绿色只用于操作和强调，不承载大段正文。
- 触控目标不小于 44px；底部主按钮避开安全区。
- 深色模式不反转图片，用深色蒙层和文本颜色保证封面可读。
- 所有删除、取消关注、退出登录统一使用二次确认；AI 生成中允许查看进度、取消或转为后台。

## Figma 交付结构

1. `00 · Foundations`：颜色、字体、间距、圆角、阴影、图标规则。
2. `01 · Components`：标题栏、按钮、筛选、路线摘要、时间线、卡片、底部栏、状态组件。
3. `02 · Core flows`：发现、规划、AI 对话、生成结果、行程详情、地图、当前行程。
4. `03 · Community & account`：行程、发布、消息、个人中心、登录、设置及辅助页。
5. 每个核心页面同时提供浅色与深色关键状态，并标注主操作、异常状态与跳转关系。
