# 拾路派部署指南

## 一、部署架构

项目后端统一位于 `ai-travel-backend` Maven 多模块工程：

```text
微信小程序
    │ /api/v1/*
    ▼
travel-service :8080 ─────┐
                          ├── MySQL travel_app
admin-service  :8081 ─────┘
    ▲
    │ /api/v1/admin/*
admin-frontend :80
```

- `travel-service`：小程序、AI、行程、地图、社区、聊天等业务。
- `admin-service`：用户、内容、行程、积分和系统运营管理。
- `common`：公共响应、异常与共享模型。
- `admin-frontend`：React 管理后台，由 Nginx 提供静态资源和 API 代理。

## 二、准备环境变量

敏感配置不得写入镜像或提交至 Git。部署平台至少需要配置：

```text
DB_URL
DB_USERNAME
DB_PASSWORD
DEEPSEEK_API_KEY
TMAP_KEY
PIXABAY_API_KEY
WECHAT_APP_ID
WECHAT_APP_SECRET
JWT_SECRET
ADMIN_JWT_SECRET
SPRING_PROFILES_ACTIVE=prod
```

用户端和管理端必须使用不同的 JWT 密钥。

## 三、初始化数据库

新环境执行：

```bash
mysql -u root -p < sql/full-init.sql
```

已有环境根据实际版本执行 `sql/migrations/` 中的增量脚本。生产操作前请先备份数据库。

## 四、构建后端镜像

构建上下文必须是 `ai-travel-backend`：

```bash
cd ai-travel-backend

docker build \
  --build-arg BUILD_SERVICE=travel-service \
  -t shilupai/travel-service:latest .

docker build \
  --build-arg BUILD_SERVICE=admin-service \
  -t shilupai/admin-service:latest .
```

服务端口：

- `travel-service`：`8080`
- `admin-service`：`8081`

健康检查：`GET http://<travel-service>:8080/api/health`

## 五、构建管理后台

```bash
docker build -t shilupai/admin-frontend:latest ./admin-frontend
```

部署时通过 `ADMIN_API_URL` 指向管理服务，例如：

```text
ADMIN_API_URL=http://admin-service:8081
```

管理后台容器默认监听 `80` 端口。

## 六、微信云托管

建议创建三个服务：

1. `travel-service`：部署旅行服务镜像，监听 `8080`。
2. `admin-service`：部署管理服务镜像，监听 `8081`，仅允许管理后台访问。
3. `admin-frontend`：部署管理端镜像，监听 `80`。

两个 Java 服务连接同一 MySQL 实例。小程序端在 `miniprogram/config/constants.js` 中配置云环境 ID、服务名和云托管地址。

生产环境建议：

- 使用云平台密钥管理或加密环境变量。
- 限制 MySQL 公网访问和安全组来源。
- 启用 HTTPS，并关闭生产 Swagger。
- 为三个服务分别设置日志、监控和资源限额。
- 部署新版本前完成数据库备份和回滚验证。

## 七、本地运行

复制安全配置模板：

```powershell
Copy-Item ai-travel-backend/travel-service/src/main/resources/application.example.yml ai-travel-backend/travel-service/src/main/resources/application.yml
Copy-Item ai-travel-backend/admin-service/src/main/resources/application.example.yml ai-travel-backend/admin-service/src/main/resources/application.yml
```

启动旅行服务：

```bash
cd ai-travel-backend
mvn -pl travel-service -am spring-boot:run
```

另开终端启动管理服务：

```bash
cd ai-travel-backend
mvn -pl admin-service -am spring-boot:run
```

启动管理后台：

```bash
cd admin-frontend
npm ci
npm run dev
```

本地地址：

- 小程序后端：`http://localhost:8080`
- 管理服务：`http://localhost:8081`
- 管理后台：`http://localhost:3000/admin`
