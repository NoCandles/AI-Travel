# 微信云托管部署指南

## 一、架构说明

```
                        微信小程序
                            │
                            ▼
                    ┌───────────────┐
                    │  微信云托管     │
                    │               │
                    │  ┌─────────┐  │
                    │  │ 服务1    │  │─── travel-backend  (端口8080)
                    │  │小程序后端│  │    API: /api/trips /api/auth ...
                    │  └─────────┘  │
                    │               │
                    │  ┌─────────┐  │
                    │  │ 服务2    │  │─── admin-backend   (端口8081)
                    │  │管理后台  │  │    API: /api/admin/users ...
                    │  └─────────┘  │
                    │               │
                    │       ↕       │
                    │  ┌─────────┐  │
                    │  │  MySQL   │  │─── travel_app (共享数据库)
                    │  └─────────┘  │
                    └───────────────┘
```

两个服务共享同一个 MySQL 数据库 `travel_app`，各司其职：
- **travel-backend (8080)**: 处理小程序端所有业务请求
- **admin-backend (8081)**: 处理后台管理端所有请求，独立认证体系

## 二、部署步骤

### 2.1 构建 Docker 镜像

```bash
# 构建小程序后端镜像
cd travel-backend
docker build -t shilupai-travel-backend:latest .

# 构建管理后台镜像
cd ../admin-backend
docker build -t shilupai-admin-backend:latest .
```

### 2.2 推送到微信云托管

在微信云托管控制台中：

1. **创建服务1 — travel-backend**
   - 服务名称: `travel-backend`
   - 镜像: `shilupai-travel-backend:latest`
   - 端口: `8080`
   - 环境变量:
     ```
     SPRING_PROFILES_ACTIVE=prod
     ```

2. **创建服务2 — admin-backend**
   - 服务名称: `admin-backend`
   - 镜像: `shilupai-admin-backend:latest`
   - 端口: `8081`
   - 环境变量:
     ```
     (使用 application.yml 中配置，无需额外环境变量)
     ```

### 2.3 数据库配置

两个服务都需要连接同一个 MySQL。在微信云托管中：

- 使用云托管的 MySQL 实例
- 确保 `application.yml` 中的数据库连接配置指向正确的 MySQL 地址
- 执行迁移脚本: `sql/migrations/add_admin_system.sql`

### 2.4 小程序端配置

小程序需要访问两个服务的域名，在 `miniprogram/utils/request.js` 中：

```javascript
// 小程序后端 API
const TRAVEL_BASE_URL = 'https://travel-backend-xxxxx.sh.gfp.tencent-cloud.com';
// 管理后台 API（如果需要在管理端小程序中访问）
const ADMIN_BASE_URL = 'https://admin-backend-xxxxx.sh.gfp.tencent-cloud.com';
```

## 三、独立部署 vs 单服务部署

### 方案A: 双服务独立部署（推荐）

```
优点:
  - 两个服务独立扩缩容
  - 管理后台挂了不影响小程序
  - 各自独立日志和监控
  - 符合微服务设计原则

缺点:
  - 需要维护两个容器
  - 云托管费用略高
```

### 方案B: 合并到同一个服务

如果你希望合并到一个服务（更简单）：

将 admin-backend 的代码合并到 travel-backend 中作为子模块：
```
travel-backend/src/main/java/com/ai/travel/
├── controller/      # 小程序API
├── admin/
│   └── controller/  # 管理后台API
└── ...
```

只需要一个容器即可：
- travel-backend 同时监听 `/api/*`（小程序）和 `/api/admin/*`（管理后台）
- 共享同一个端口 8080

## 四、本地开发

```bash
# 启动双服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止
docker-compose down
```

- 小程序后端: http://localhost:8080
- 管理后台: http://localhost:8081
